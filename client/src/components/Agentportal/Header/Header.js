import React from "react";
import "./Header.css";
import Navbar from "./Navbar/Navbar";

const Header = (props) => {
  return (
    <header>
      <Navbar />
    </header>
  );
};

export default Header;
